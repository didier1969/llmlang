/target
/vendor/z3
*.zip
.lll-cache/
/build
# lsp buffer temp (REQ-LLL-160) — sibling of the edited file, normally auto-removed
.lll-lsp-*

# Agent worktrees / local tooling state
.claude/

# devenv / nix — état local ignoré, devenv.lock committé (reproductibilité)
/.devenv/
/.devenv.flake.nix
/.direnv/
