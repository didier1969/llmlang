# Test fixture for REQ-LLL-053 (4): a HYPHENATED Cargo package name (common on
# crates.io, e.g. `wasm-bindgen`) — Rust always exposes it as an underscored
# module path (`ffi_hyphen_fixture::double`), never the hyphenated form.
[package]
name = "ffi-hyphen-fixture"
version = "1.0.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[workspace]
