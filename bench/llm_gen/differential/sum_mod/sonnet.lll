module SumMod:
  part sum(xs: List[Int]) -> Int:
    match xs:
      [] -> yield 0
      h :: t -> yield h + sum(t)
  part sum_mod(xs: List[Int], m: Int) -> Int:
    requires m > 0
    ensures 0 <= result
    ensures result < m
    yield sum(xs) mod m
