module M:
  part reduceDiv(xs: List[Int], acc: Int) -> Int:
    measure length(xs)
    match xs:
      [] -> yield acc
      h :: t ->
        match h:
          0 -> yield acc
          _ -> yield reduceDiv(t, acc div h)
