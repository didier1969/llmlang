Write a part `max3(a: Int, b: Int, c: Int) -> Int` returning the maximum of
three integers with a full contract (result >= each argument, result equals
one of them). Add main() via IO printing max3(3, 9, 5).
