Write `all_pos() -> Array[Int]` returning `array(1, 2, 3)`, with a contract
using a BOUNDED UNIVERSAL quantifier (`forall i in 0 .. length(result): ...`)
proving every element is strictly positive. Then write
`first7(a: Array[Int]) -> Int` that requires the array is non-empty AND that a
BOUNDED EXISTENTIAL (`exists i in 0 .. 1: get(a, i) == 7`) holds, ensures the
result equals 7, and yields the element at index 0. `main()` prints the element
at index 0 of `all_pos()`.
