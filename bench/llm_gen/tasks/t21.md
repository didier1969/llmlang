Persist and reload data through the normalized `Db` effect (import
`std/db.lll`). Write `flag(b: Bool) -> Int` (1/0). In `main() -> Int via IO, Db`,
open an in-memory database (`Db.open(":memory:")`), create a table `t (v INTEGER)`,
insert the value 42, then `Db.query` it back, turn the JSON result into rows with
`unarr`, and print `flag(count(rows) == 1)` (the round-trip fact). The domain stays
pure; the DB plumbing lives at the `Db` effect boundary.
