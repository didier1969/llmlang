Write `head(xs: List[Int]) -> Int` returning the first element, REQUIRING the
list to be non-empty (hint: `not (xs == [])` is a valid contract). Your match
does NOT need a `[]` arm — the verifier must prove it unreachable from the
precondition. main() prints head([7, 8]).
