Use the built-in tail-resumptive `State` effect (`State.get()` / `State.put(n)`).
Write `bump(by: Int) -> Int via State` that adds `by` to the cell and yields the
new value. Write `total() -> Int via State` that calls `bump(10)`, `bump(20)`,
then yields `bump(12)` (all sharing one cell). Write a PURE `run() -> Int` that
installs the canonical handler with `handle total() with State from 0:` /
`return r -> yield r`. `main()` prints `run()` (expected 42).
