module Gen.T11:

  part last(xs: List[Int], d: Int) -> Int:
    match xs:
      []     -> yield d
      h :: t ->
        match t:
          []  -> yield h
          _   -> yield last(t, d)

  part main() -> Int via IO:
    let l = IO.print(last([1, 2, 9], 0))
    yield l
