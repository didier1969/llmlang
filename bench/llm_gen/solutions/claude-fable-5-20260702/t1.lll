module Gen.T1:

  part abs_val(x: Int) -> Int:
    ensures result >= 0, result == x or result == 0 - x
    match x:
      v when v < 0 -> yield 0 - v
      _            -> yield x

  part main() -> Int via IO:
    let a = IO.print(abs_val(0 - 42))
    yield a
