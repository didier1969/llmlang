module Gen.T2:

  part len(xs: List[Int]) -> Int:
    ensures result >= 0
    match xs:
      []     -> yield 0
      h :: t -> yield 1 + len(t)

  part main() -> Int via IO:
    let n = IO.print(len([3, 1, 4, 1, 5]))
    yield n
