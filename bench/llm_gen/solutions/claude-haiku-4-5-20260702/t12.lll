module AbsDiff:

  part abs_diff(a: Int, b: Int) -> Int:
    ensures result >= 0
    ensures result == a - b or result == b - a
    match a:
      _ when a >= b -> yield a - b
      _ -> yield b - a

  part main() -> Int via IO:
    let d = abs_diff(3, 11)
    yield IO.print(d)
