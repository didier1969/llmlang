module AbsVal:

  part abs_val(x: Int) -> Int:
    ensures result >= 0
    ensures result == x or result == 0 - x
    match x:
      _ when x >= 0 -> yield x
      _ -> yield 0 - x

  part main() -> Int via IO:
    let r = abs_val(0 - 42)
    yield IO.print(r)
